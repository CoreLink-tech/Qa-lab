// Deliberately missing "run" -- used to test that loadRules() fails
// loudly and clearly instead of silently breaking downstream.
module.exports = {
    id: "T3",
    name: "Test Rule 3",
    category: "Test"
};
